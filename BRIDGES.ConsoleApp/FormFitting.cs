using BRIDGES.DataStructures.PolyhedralMeshes.Abstract;
using BRIDGES.DiscreteElementModelling.Model;
using System;
using System.Collections.Generic;

namespace BRIDGES.ConsoleApp
{
    internal class FormFitting
    {
        static void Main(string[] args)
        {
           // Mesh mesh = new DataStructures.PolyhedralMeshes.Abstract.Mesh();
           // Model model = new Model(mesh);

            //List<double> forceDensities = new List<double>();

            //model.UpdateForceDensities(forceDensities);
            

            Console.WriteLine("Hello World!");
        }
    }
}
